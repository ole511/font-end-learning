<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <div>
        <label>Number A:<input id="radio-a" type="radio" name="math-obj" value="a"></label><input id="num-a"
            type="text">
        <label>Number B:<input id="radio-b" type="radio" name="math-obj" value="b"></label><input id="num-b"
            type="text">
    </div>
    <div>
        <button>判断当前选中的输入框输入内容是否为数字</button>
        <button>把 A 四舍五入为 B 个小数位数的数字</button>
        <button>当前选中数字的绝对值</button>
        <button>对当前选中的数字进行上舍入</button>
        <button>对当前选中的数字进行下舍入</button>
        <button>把当前选中的数字四舍五入为最接近的整数</button>
        <button>返回 A 和 B 中的最高值</button>
        <button>返回 A 和 B 中的最低值</button>
    </div>
    <p id="result"></p>

    <script>
        var radioA = document.querySelector('#radio-a');
        var radioB = document.querySelector('#radio-b');
        var numA = document.querySelector('#num-a');
        var numB = document.querySelector('#num-b');
        var buttons = document.querySelectorAll('div button');
        var result = document.querySelector('#result');

        function judgeNum(num){
            if (Number(num)){
                return true;
            }else{
                console.log(num + '不是数字，请重新输入！');
                return false;
            }
        }

        buttons[0].onclick = function(){
            if (radioA.checked == true){
                if(judgeNum(numA.value)){
                    result.innerHTML = '当前输入框中的内容是数字。'
                }else{
                    result.innerHTML = '当前输入框中的内容不是数字。'
                }
            }else if(radioB.checked == true){
                if(judgeNum(numB.value) == true){
                    result.innerHTML = '当前输入框中的内容是数字。'
                }else{
                    result.innerHTML = '当前输入框中的内容不是数字。'
                }
            }else{
                result.innerHTML = '请选择输入框。'
            }
        } 


        buttons[1].onclick = function(){
            if(judgeNum(numA.value) && judgeNum(numB.value)){
                var num = Number(numA.value);
                var decimal = Number(numB.value);
                result.innerHTML = num.toFixed(decimal);
            }
        }

        buttons[2].onclick = function(){
            if(radioA.checked == true){
                if(judgeNum(numA.value)){
                    result.innerHTML = Math.abs(numA.value);
                }
            }
        }

        buttons[3].onclick = function(){
            if(radioA.checked == true){
                if(judgeNum(numA.value)){
                    result.innerHTML = Math.ceil(numA.value);
                }
            }
        }
        buttons[4].onclick = function(){
            if(radioA.checked == true){
                if(judgeNum(numA.value)){
                    result.innerHTML = Math.floor(numA.value);
                }
            }
        }

        buttons[5].onclick = function(){
            if(radioA.checked == true){
                if(judgeNum(numA.value)){
                    result.innerHTML = Math.round(numA.value);
                }
            }
        }

        buttons[6].onclick = function(){
            if(judgeNum(numA.value) && judgeNum(numB.value)){
                result.innerHTML = Math.max(numA.value, numB.value);
            }
        }
        
        buttons[7].onclick = function(){
            if(judgeNum(numA.value) && judgeNum(numB.value)){
                result.innerHTML = Math.min(numA.value, numB.value);
            }
        }

    </script>
</body>
</html>
